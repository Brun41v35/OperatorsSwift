import UIKit

// Atribuicao "="
var name = "Bruno"
print(name)

// Operadores: +,-,*,/
var (number, number1) = (10,20)
let result = number + number1
print(result)

// Operadores compostos
number += number1
print(number)

// Operador Ternario - Ele atua em tres opcoes
var nota = 7.00
var resultadoNota = (nota >= 7.00) ? "Aprovado" : "Reprovado"
